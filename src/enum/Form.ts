export enum Sortby {
    age = 'age',
    date = 'date'
}

export enum Gender {
    female = 'female',
    male = 'male',
    none = '_none'
}

export enum YesNo {
    yes = '1',
    no = '0',
    none = '_none'
}